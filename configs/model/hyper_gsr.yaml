name: hyper_gsr

# ------------------------------
# Target-Edge Initializer
# ------------------------------
target_edge_initializer:
  num_heads: 4
  edge_dim: 1
  dropout: 0.2
  beta: false

# ------------------------------
# Hyper-Dual Learner
# ------------------------------
hyper_dual_learner:
  in_dim: 1
  hidden_dim: 32
  dropout: 0.1
  mode: trans            # Use TransformerConv for bidirectional bipartite message passing
  heads: 4
  edge_dim: 0            # **Enable** edge_attr dimension=1 (use distance as attribute for each incidence edge)

  # ---- geometry priors (new) ----
  use_edge_distance: false
  edge_geo_dim: 8
  dist_norm: zscore
  use_hyper_coords: false

  # ---- shrink output ----
  use_shrink_output: 
  shrink_threshold: 0.01
